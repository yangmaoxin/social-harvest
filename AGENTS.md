# Codex 协作准则

用于减少 AI 编程协作中的常见错误。可与项目自己的约定一起使用。

**取向：** 这套准则偏向稳妥、清晰、可验证，而不是一味求快。对明显简单的一行修改，可以适当从简。

## 1. 先理解，再动手

**不要脑补需求。不要掩盖困惑。把假设和取舍说出来。**

开始实现前：
- 先阅读当前上下文，再下结论。
- 明确说出关键假设；拿不准就问。
- 如果需求可能有多种解释，先列出来，不要默默选一种。
- 如果发现更简单的方案，直接提出，不必机械执行复杂方案。
- 如果信息不足以安全落地，先停下来说明缺口。

对 Codex 来说，这意味着：
- 先看现有代码、文档、测试、配置，再改。
- 不根据名字猜接口、不根据感觉猜架构。
- 用户说“修一下”时，先定位“坏在哪里”，再决定怎么修。

## 2. 简洁优先

**只写解决当前问题所需的最少代码。不预支复杂度。**

- 不顺手加需求外功能。
- 单次使用的逻辑，不为了“优雅”提前抽象。
- 没要求就不加“通用化”“插件化”“可配置化”。
- 不为极低概率且没有证据的场景堆大量防御代码。
- 如果 50 行能解决，就不要写成 200 行。

优先级顺序：
1. 正确
2. 简单
3. 再考虑可扩展

自检问题：
- 这段代码是不是比问题本身还复杂？
- 一个熟悉该项目的工程师会不会觉得这是过度设计？
- 有没有更小的改法也能达到同样结果？

## 3. 精准修改

**只改任务直接涉及的部分。只清理自己引入的冗余。**

修改已有代码时：
- 不顺手重构旁边的代码。
- 不顺手统一格式、改命名、换风格、补类型，除非任务确实需要。
- 保持现有项目风格，不把个人偏好带进来。
- 发现无关问题可以提示，但不要顺便修。

因为本次改动产生的无用内容，应当一起清掉：
- 不再使用的 import
- 已经失效的局部变量
- 被当前改动替代掉的小函数或分支

但不要删除这些内容：
- 改动前就存在、且和任务无关的死代码
- 你还没看懂的注释、逻辑或配置
- 只是“看起来能优化”的旧实现

检验标准：
- 每一处 diff 都能直接解释为“这是为了完成当前请求”。

## 4. 目标驱动，不靠感觉收尾

**先定义完成标准，再迭代到验证通过。**

把模糊请求改写成可验证目标：
- “加校验” -> “补无效输入测试，并让测试通过”
- “修 bug” -> “先复现，再修复，再验证不回归”
- “重构这块” -> “保证行为不变，并让相关测试保持通过”
- “优化性能” -> “确认瓶颈指标，并验证目标指标改善”

多步骤任务尽量写成：

```text
1. 定位问题 -> 验证：能稳定复现
2. 实现改动 -> 验证：目标测试通过
3. 回归检查 -> 验证：相关旧测试仍通过
```

如果没有测试，也要给出最小可验证方式：
- 运行相关命令
- 手动复现步骤
- 样例输入输出
- 日志或断言变化

不要用“应该可以了”收尾，要用“我怎么确认它真的好了”收尾。

## 5. 先贴近项目，再考虑理想写法

**优先融入现有系统，而不是把局部改成你心中的新标准。**

- 沿用项目已有目录结构、命名习惯、错误处理方式、测试风格。
- 新代码先兼容周围上下文，再追求抽象上的整齐。
- 如果现有模式不完美，但任务不要求重构，就先保持一致。

只有在下面情况才主动升级模式：
- 旧写法会直接妨碍本次需求
- 不改会引入明显 bug 或维护风险
- 用户明确要求重构或统一

## 6. 先证据，后结论

**不要凭印象判断代码。尽量用文件、命令、测试和输出来支撑结论。**

- 说“这里没被使用”前，先搜引用。
- 说“测试挂了”前，先跑相关测试。
- 说“这是性能瓶颈”前，先给出已有指标、日志或可观察证据。
- 如果暂时无法验证，要明确说这是推断，不要说成事实。

## 7. 沟通要短，但要把关键风险说透

**默认高效推进；只有在分歧明显、影响较大、风险较高时才升级沟通。**

优先自己推进的情况：
- 需求足够清楚
- 影响面有限
- 有明显的局部最优解

需要先确认的情况：
- 有多种实现路径且后果不同
- 会改公共接口、数据结构、持久化格式
- 可能破坏兼容性
- 需要大范围重构

确认时不要泛泛而问，尽量给出选项、取舍和建议。

## 8. Codex 执行细则

- 搜索优先用 `rg`。
- 先做最小范围阅读，再做最小范围修改。
- 能局部验证就不要一上来跑整套重测试。
- 修改后说明做了什么、怎么验证、还有什么没验证。
- 如果环境限制导致无法验证，要明确说出来。

## 9. 项目运行时约束

- 每次开始本项目任务前，先检查当前 Node.js 版本是否满足 `package.json` 的 `engines.node`。如果不匹配，先检查用户环境是否有 `nvm`；有则切换到匹配版本，再运行 `npm install`、测试、打包等命令。没有 `nvm` 或无法切换时，先说明风险并征求用户确认。
- 新增命令、任务编排、开发辅助脚本一律使用 Node.js。
- 不新增 `.sh`、`.bash`、PowerShell、Python 编排脚本，除非用户明确要求且文档说明原因。
- `package.json` scripts 应优先调用 `node scripts/<name>.js` 或已有 npm 命令。
- 面向桌面端、安装版 CLI、定时任务的入口必须保持跨平台可运行，不能依赖 Git Bash、WSL、系统 shell 或 Python 虚拟环境。
- 如需执行外部命令，优先在 Node 中使用 `spawn` / `spawnSync`，并显式处理退出码、工作目录、环境变量和错误输出。

## 10. 采集展示口径

- `--display detailed` 在本项目里是给人看的详细进度，不是原始调试输出。不要为了减少 JSON 噪音把已有一线命令从 `detailed` 改成 `compact`。
- 如果用户说“终端/远端输出看不懂”“不要展示 JSON”，正确做法是过滤或转换原始 JSON/stdout，只保留人类可读进度、摘要和报告路径。
- 原始采集 JSON、`TASK_EVENT`/`OPENCLI_PROGRESS` 明细和排障细节应写入 `daily-report.json`、`task-report.json`、`task-events.jsonl` 或平台产物文件，由 AI 读取后转成业务语言汇报。
- `share:run` 默认也应只推送人类可读展示事件；`--passthrough` 和 `--include-raw-output` 只用于调试，不要作为普通用户入口。

---

如果这份准则生效，通常会看到：
- 需求澄清发生在写代码之前
- diff 更小、更干净
- 很少出现“顺手做了很多”
- 结果更容易验证，而不是只能凭感觉判断
